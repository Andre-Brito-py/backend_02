# Backend environment variables (Neon + Render)
PORT=4000
# Exemplo Neon: postgresql://usuario:senha@ep-xxxx.aws.neon.tech/seubanco?sslmode=require
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
JWT_SECRET=supersecretjwtkeychangeit
# Origens permitidas (separadas por vírgula)
# Inclua seu domínio Vercel e localhost em desenvolvimento
CORS_ORIGINS=https://your-frontend.vercel.app,http://localhost:3000
